---
description: vegoo-web-admin 项目规范（技术栈、目录、命名、风格；含小模块 config 与 Mock 按接口开关约定）
alwaysApply: true
---

# vegoo-web-admin 项目规范

## 技术栈

- **框架**: Vue 3 (Composition API) + TypeScript
- **构建**: Vite 7，Node >= 20.19.0，pnpm >= 8.8.0
- **UI**: Element Plus（按需）、Tailwind CSS 4、SCSS（@styles 变量与 mixin 已全局注入）
- **状态/路由**: Pinia、Vue Router 4
- **请求**: 封装在 `@/utils/http`，基于 axios
- **国际化**: vue-i18n，文案 key 如 `menus.system.user`
- **自动导入**: unplugin-auto-import（vue/router/pinia/@vueuse/core）、unplugin-vue-components（Element Plus）

## 路径别名（tsconfig + vite 一致）

| 别名 | 指向 |
|------|------|
| `@/*` | `src/*` |
| `@views/*` | `src/views/*` |
| `@imgs/*` | `src/assets/images/*` |
| `@icons/*` | `src/assets/icons/*` |
| `@utils/*` | `src/utils/*` |
| `@stores/*` | `src/store/*` |
| `@plugins/*` | `src/plugins/*` |
| `@styles/*` | `src/assets/styles/*` |

导入时优先使用别名，例如：`import { useUserStore } from '@/store/modules/user'`、`@/api/auth`。

## 代码风格（ESLint + Prettier）

- **引号**: 单引号 `'`
- **分号**: 不使用分号
- **缩进**: 2 空格，不用 Tab
- **行宽**: 100 字符（Prettier printWidth）
- **尾逗号**: 无（trailingComma: none）
- 使用 `const`/`let`，禁止 `var`
- 连续空行最多 1 行

## 数值展示（小数位数）

- 页面内**度量类数值**（金额、比率、百分比、ROI、ROAS、CPI、图表坐标轴与 tooltip 等）若存在小数部分，展示时统一**保留小数点后两位**（推荐 `toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })`，或与项目内统一的 `formatNum2` / `formatUsd2` 等工具一致）。
- **例外**：接口已约定为**预格式化字符串**的字段（如部分 KPI 卡片上的花费/收入文案）可原样展示，避免与后端格式冲突；**纯整数/计数类**（如安装量、条数、页码）不强制补 `.00`。

## 术语与文案统一（非常重要）

新开发/新页面/新模块涉及字段展示、筛选项、表头、占位符、按钮、i18n 文案时，必须统一使用以下术语（避免同一概念多种叫法，影响联调与认知一致性）：

- **广告平台**：全平台范围内，UI 文案中原“渠道”一律改为“广告平台”
- **优化师**：UI 文案中原“优化师”一律改为“优化师”
- **预算**：全平台范围内，UI 文案中原“计算消耗”一律改为“预算”

适用范围包括但不限于：`menus.*`、页面标题、表格列 `label`、筛选表单 `label/placeholder`、统计卡片字段名、导出字段名等。

## 国旗与 flag-icons

- 国家 **ISO 3166-1 alpha-2** 代码：**契约与联调 JSON** 统一 **`countryCode`**（无下划线）。由接口返回即可；**不要**在接口或 Mock 中返回国旗 emoji、国旗图片 URL 等纯展示字段。
- 前端使用项目依赖 **flag-icons**：在需要展示国旗的页面/组件或**页面入口** `index.vue` 中 `import 'flag-icons/css/flag-icons.min.css'`（入口引入可保证样式与 `url(../flags/...)` 经构建正确解析），用 `<span :class="'fi fi-' + 小写码"></span>` 或与 `class="fi fi-xx"` 等价写法。若代码非法或为空，则不渲染国旗。
- 历史别名（如 `UK`）可在前端映射为 `gb` 等标准码后再套 `fi-*` 类名。

## 字段命名统一（重要）

后续开发中（新开发或重构）涉及 **接口入参/出参类型**、**请求参数**、**表格列 prop**、**筛选表单 v-model**、**Mock 契约（mock/backend-api）** 时，字段名必须统一：

- **`mock/backend-api` 与 HTTP 业务 JSON**：**全部键名 camelCase，禁止下划线**（见 `backend-fields.mdc`「接口契约与 HTTP JSON 命名」）。
- **HTTP POST 请求体（筛选/维度）**：凡页面或接口**实际提供**的筛选，键名须与 **「接口请求 · 统一筛选维度键名」**一致（如 **`appId`**、**`platform`**、**`source`**、**`countryCode`**、**`account`**）；「全部」为 **`\"\"`**。**未**在 UI 出现的维度不要写进契约或 POST body（见 `backend-fields.mdc` 该节「与页面一致」）。
- **应用筛选**：勿单独用 `app`、`appPackage` 作为应用筛选主字段。细则见 `backend-fields.mdc`「接口请求 · 应用筛选（appId）」。
- **筛选入参中的枚举/维度**：请求体中类型为 **`string`**（如 `\"1\"`），「不限」用 **`\"\"`**；勿传 JSON number。细则见 `backend-fields.mdc`「接口请求 · 筛选「全部」」与「接口请求 · 筛选字段类型（枚举 string）」。

### 常见 Lint 与格式注意点（避免重复报错）

- **模板禁止 TypeScript 语法**：`<template>` 里不能写 `as` 类型断言（如 `:filter="(x as Type)"`），会触发 vue/no-parsing-error。需在 script 里用 `computed(() => x as Type)` 得到值后再绑定到模板。
- **行宽 100 字符**：单行超长会被 Prettier/ESLint 要求换行。需拆行的典型场景：
  - 模板中的三元表达式（如 `:title="a ? b : c"` 过长时改为多行，运算符换行）。
  - 组件多属性（如 `ElSelect` 的 `v-model`、`:placeholder`、`style` 等拆成多行）。
  - `ElFormItem` 的 `:label`、`prop` 等过长时拆行。
  - 表单校验 `rules` 中对象字面量（如 `{ required: true, message: '...', trigger: '...' }`）过长时，将对象或数组项拆成多行。
  - 常量对象（如 `STATUS_CONFIG`、列配置）中单行过长的项要换行。
- **script 中使用的 Element 组件须显式导入**：在 `formatter`、`h()` 渲染或普通 JS 里用到的（如 `ElTag`、`ElMessageBox`、`ElMessage`）需 `import { ElTag, ElMessageBox, ElMessage } from 'element-plus'`，否则 ESLint 报 “is not defined”（模板里用的已自动导入，可不写）。
- **defineProps 未使用勿赋值**：若只在模板里用 props、script 里不引用，不要写 `const props = defineProps<...>()`，否则报 “assigned but never used”。只写 `defineProps<...>()` 即可。
- **Promise 链式调用格式**：`.then(...).catch(...)` 需按 Prettier 换行，例如写成 `.then(...)` 换行后 `.catch(...)`，并保持缩进一致。
- **.vue 默认导出**：已在 `src/env.d.ts` 中声明 `declare module '*.vue'`，所有 `.vue` 文件均视为有 default export；无需在各组件内再写单独的 `<script lang="ts"> export default { name } </script>`。
- **多行 import / type**：同一 import 或 `import type { A, B, C }` 项过多、超行宽时，按 Prettier 拆成多行（如 type 每项一行或合理折行）。

## 类型与 API

- **全局 API 类型**: 使用命名空间 `Api.*`，定义在 `src/types/api/api.d.ts`，无需 import（如 `Api.Auth.LoginParams`、`Api.SystemManage.UserListItem`）。
- **后端字段与业务概念**: 与报表、用户增长、广告数据等相关的字段命名与语义须遵循规则 `backend-fields.mdc`（数据字典），定义类型、API、表格列、筛选器时与该表一致。
- **通用类型**: 从 `@/types` 或 `@/types/router`、`@/types/store` 等按需导入。
- **接口请求**: 使用 `request.get/post/put/delete`（来自 `@/utils/http`），方法命名以 `fetch` 开头，如 `fetchLogin`、`fetchGetUserList`；泛型使用 `Api.*` 类型。

### 用户增长 API 目录拆分（`src/api/user-growth/`）

- **位置**：用户增长相关 `fetch*` 放在 **`src/api/user-growth/`** 目录下，按子业务**分文件**，**不要**再使用单文件 `src/api/user-growth.ts`。
- **粒度**：**一个子业务对应一个 `*.ts` 文件**，文件名使用 kebab-case，与 `views/user-growth/<子目录>` 或业务语义对齐，例如：
  - `conversion-management.ts` — 转化管理
  - `overall-recovery.ts` — 整体回收
  - `comprehensive-analysis.ts` — 综合分析
  - `platform-analysis-detail.ts` — 广告平台分析详情（从综合分析钻取）
  - `alert-management.ts` — 预警管理
  - `ad-platform-analysis.ts` — 广告平台分析大屏
  - `my-ads.ts` — 我的广告
  - `ad-performance.ts` — 广告成效（含广告系列详情分片）
  - `my-performance.ts` — 我的绩效
- **聚合入口**：`src/api/user-growth/index.ts` 使用 `export * from './...'` 汇总导出，使 **`import { ... } from '@/api/user-growth'`** 仍解析到该目录的 `index.ts`（与 `views` 下 `user-growth` 模块一一对应）。
- **按需引用**：只依赖单个子业务时，优先 **`import { ... } from '@/api/user-growth/<文件名>'`**（无 `.ts` 后缀），减少 barrel 耦合、职责更清晰。
- **路径常量与子模块 config**：若某子业务需要**独立网关前缀**（例如广告成效），在 **`src/views/user-growth/<页面>/config/api-base.ts`**（或同目录 `data-source.ts` 旁）维护常量；`src/api/user-growth/*.ts` 中 `fetch*` 从该 config 引用。**不要**为单个子业务改全局 `analysis-api-base.ts` 而牵连其他模块的 URL。

## 页面与组件

- **页面入口**: 每个业务模块在 `src/views/<模块>/<页面>/index.vue`，路由 component 配置为路径形式如 `'/system/user'`，对应 `src/views/system/user/index.vue`。
- **子组件**: 放在同目录下 `modules/`，如 `views/system/user/modules/user-dialog.vue`；文件名 kebab-case，组件名 PascalCase。
- **组件名**: 必须在 `<script setup>` 中写 `defineOptions({ name: 'PascalCaseName' })`，与路由 name 或功能一致，例如 `ResultSuccess`、`UserRightPanel`；公共组件使用 `Art` 前缀（如 `ArtTable`）。
- **模板**: 使用 Element Plus 组件时用自动导入的短名（如 `ElButton`）；项目内公共组件如 `ArtTable`、`ArtResultPage` 等按项目已有用法引用。

## 组件设计结构（概要）

- **详细约定** 见规则 `component-design.mdc`（编辑 .vue 或 components/、views 下 composables 时生效）。
- **目录**: 公共组件在 `src/components/core/<分类>/<组件名>/index.vue`，可含 `style.scss`、`composables/`、`widget/`；页面子组件在 `views/<模块>/<页面>/modules/`。
- **Props/Emits**: 用 TypeScript 接口 + `defineProps<Props>()`、`defineEmits<Emits>()`，默认值用 `withDefaults`；v-model 用 `update:visible` + computed get/set。
- **Slots**: 具名插槽（#content、#buttons）或动态插槽；需要父调子时用 `defineExpose({ method, ref })`。
- **逻辑拆分**: 复杂组件用同目录 `composables/`；页面数据与请求用 `views/.../composables/`（如 `useCockpitData`）。

## 路由

- **类型**: 使用 `AppRouteRecord`、`RouteMeta`（来自 `@/types/router`）。
- **模块化**: 路由按业务拆在 `src/router/modules/*.ts`，在 `modules/index.ts` 中合并为 `routeModules`。
- **meta**: `title` 使用 i18n key（如 `menus.system.user`），`icon` 使用 Iconify 码（如 `ri:user-3-line`）；可配置 `keepAlive`、`roles`、`authList`、`isHide`、`isHideTab` 等。

## 目录约定

- `src/api`：按业务拆分接口（如 `auth.ts`、`system-manage.ts`）；**用户增长**见上文「用户增长 API 目录拆分」，目录为 `src/api/user-growth/`。
- `src/store`：Pinia 模块，如 `modules/user`。
- `src/utils`：公共工具（含 `http`、`table` 等）。
- `src/types`：全局类型与 API 命名空间；自动生成的导入类型在 `types/import/`。
- `src/views/<模块>/<页面>/mock`：该页面 mock 数据（如需要）。
- `src/views/<模块>/<页面>/config`：**小模块（页面/子业务）**内的 **数据源开关**（mock / 远程按**接口粒度**配置）。**必须**落在该页面目录下，**禁止**放在上一级大模块根目录与多个子业务混用（例如商业洞察下 IAA、IAP 须各有独立 `config/`，勿用 `views/business-insight/config` 一类路径统管）。细则与完整流程见 **`module-api-mock-config.mdc`**。
- `src/views/<模块>/<页面>/composables`：页面级组合式函数（如需要）。

## 数据与开发流程规范

- **先 Mock 再开发组件**：开发大模块时，先在该模块下建立/完善 **Mock 数据**（如 `views/<模块>/<页面>/mock/data.ts` 或 `mock.ts`），并同步定义好 **类型**（`types.ts` 或类型与 mock 同文件）。待大模块的数据结构与字段约定清晰后，再进行页面入口与 `modules/` 内子组件的开发；组件内优先用 mock 做展示与联调，接口就绪后再切换为真实请求。
- **契约 + 真实请求 + 小模块 config 开关**：使用 Mock 开发时，须按 **`mock/backend-api` 契约**在 `src/api` 写好 **`fetch*`**，并在 **该小模块** `config/` 内维护开关；契约格式与协作见 **`api-contract-and-mock-conventions.mdc`**，实现细则见 **`module-api-mock-config.mdc`**。
- **导出 / 下载文件**：新增或对接「导出」类接口的 **`mock/backend-api` 契约与 `fetch*`** 时，**一律**按 **HTTP 二进制文件流**（`blob` / `requestBlob`、响应头 `Content-Type` + `Content-Disposition`）约定与实现，**禁止**把该导出 endpoint 的主响应写成「仅 JSON + Base64」等替代形态；完整条款见 **`api-contract-and-mock-conventions.mdc`**「导出 / 下载类接口（HTTP 文件流 · 硬性）」（异步任务类导出另立契约，不与即时流式导出混用）。

### Mock 与数据源开关（与 module-api-mock-config 一致）

- **放置位置**：config 放在 **`src/views/<模块>/<页面>/config/`**（如 `data-source.ts`），与**当前页面/子业务**一一对应；同一父模块下的不同子业务（如 `iap-analysis` 与 `iaa-analysis`）**各自一份 config**，互不合并到父级 `views/<模块>/config`。商业洞察示例：`business-insight/iap-analysis/config/data-source.ts`、`business-insight/iaa-analysis/config/data-source.ts`。
- **开关粒度**：**每个契约接口 / 每个 `fetch*` 对应一项开关**，便于单独切 Mock 或远程。推荐在 config 中用 **TypeScript `enum`（或 const 枚举式对象）列出端点**，再配 **`Record<Endpoint, boolean>`**（或 `'mock' | 'remote'`）集中维护默认值；`src/api` 内按枚举分支走 `request` 或 mock 实现。
- **环境变量**：**不要**在仓库根目录 `.env` / `.env.development` 等为**单个业务模块**增加 `VITE_*` 类数据源总开关；默认值以模块 `config` 内导出常量为准。若需开发机临时覆盖，在**该小模块** `config` 内自行扩展（如 localStorage），且仍须保持**每接口显式 key**。
- **禁止**：将按接口开关散落在全局 `store`、无归属 `utils`，或仅在 `.vue` 内写死 `useMock = true` 而无集中、可枚举的配置表。
- **配置管理（`src/views/config-management`）Mock 分层（不必全员统一）**：
  - **已对齐子模块**：**国家管理**、**成本系数**、**汇率管理** 采用「`src/api/config-management/` 下对应子文件（如 `country-management.ts`）内按页面 `config/data-source.ts` 的 `is*EndpointMock` 分支 + `mock/*-api-mock.ts`，页面**只**调 `@/api/config-management/<子文件>`」，不在页面内再写一套「本地数据 / 远程」双轨。
  - **其余子模块**：如订单导入、绩效配置、账户管理等，**不强制**改为上述形态；**允许**继续以页面侧本地 mock、静态或较重的本地数据为主，接真接口时再按需补契约、`fetch*` 与模块内开关。**非硬性统一要求。**
- **先查公共组件再写模块**：开发新模块或新页面时，**先检查** `src/components/core/` 下是否有可复用的公共组件（表格 `art-table`、表单 `art-form`、图表、卡片、结果页 `ArtResultPage`、搜索栏、上传等），优先使用或基于其扩展，避免重复实现；若需新能力，再考虑在公共组件上扩展或新增组件。

## 提交规范（Commitlint + cz-git）

- 使用 `pnpm commit` 或符合 Conventional Commits 的 message。
- type 枚举：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`build`、`ci`、`revert`、`chore`、`wip`。
- 示例：`feat(system): 用户管理增加导出`、`fix(auth): 登录 401 重复弹窗`。

## 样式规范（概要）

- **详细约定** 见规则 `style-conventions.mdc`（编辑 .vue/.scss/.css 时生效），以驾驶舱 cockpit 为参考。
- **配置管理深色弹窗**：`ElDialog` 公共配色变量（`--cm-dialog-*`）与 teleport + `:has` 写法见规则 `config-management-dialog.mdc`；变量定义在 `src/assets/styles/core/config-management-dialog.scss`。
- **按钮圆角**：开发时按钮统一使用**全圆角**。使用 `ElButton` 时加上 `round` 属性（如 `<ElButton round>`）；自定义按钮或需覆盖样式时使用 `border-radius: 9999px` 或 `var(--el-border-radius-round)`，保持全项目按钮风格一致。
- **页面布局**: 根节点 `{模块}-page`，全高用 `art-full-height`；栅格用 `ElRow` + `:gutter="16"`；区块间距 16px。
- **类名**: 模块前缀（如 `cockpit-`）+ BEM 风格（`block--modifier`、`is-*`）；面板用 `ElCard` + `shadow="never"`。
- **颜色**: 必须遵循下方 **颜色规范**；实现时优先使用 `src/assets/styles/core/tailwind.css` 中已定义的 CSS 变量（如 `var(--default-bg-color)`、`var(--art-primary)` 等），深色模式在 `.dark` 下生效。
- **SCSS**: `<style scoped lang="scss">`，覆盖子组件用 `:deep()`；挂到 body 的节点可单独无 scoped 块，类名带模块前缀。

### 构建稳定性约束（避免下次打包报错）

以下约束来自历史构建故障复盘，目标是让 `pnpm build` 在不同开发机上更稳定、可复现。

- **Sass 模块系统（强制）**：
  - 新增/修改 SCSS 时，**禁止**再新增 `@import`（Dart Sass 已弃用，未来版本会移除）。
  - 统一使用 `@use '...scss' as *;`（或命名空间形式 `as xxx`）。
  - 如需复用变量/mixin/函数，请在使用方显式 `@use` 引入；**不要**依赖 “某个中间文件被 @import 后全局可见” 的隐式行为。
  - 说明：本仓库的 stylelint 规则会对 `@forward` 报错（`at-rule-no-unknown`），因此**不要**在业务 SCSS 中使用 `@forward`。

- **`:deep()` 作用域（强制）**：
  - `:deep()` **只能**用于 `.vue` 的 `<style scoped>` 内（由 Vue 编译器处理）。
  - **禁止**在独立的 `.css/.scss` 文件里写 `:deep()`，否则会在生产构建的 CSS 优化阶段产生 warning / 甚至影响产物。
  - 独立样式文件需要穿透子组件时，改用正常的后代选择器（例如 `.wrap .el-input__inner`）。

- **Windows 重装依赖（推荐）**：
  - Windows 下执行 `pnpm install --force` 前，优先停止 `pnpm dev` / watcher，避免 `.node` 文件被占用导致 `ERR_PNPM_EPERM unlink ...*.node`。

- **Element Plus 类型异常排查（推荐）**：
  - 若 `vue-tsc` 成片报 `element-plus/es/index.d.ts is not a module (TS2306)`，优先排查依赖是否损坏（例如该 `index.d.ts` 变成 0 字节空文件），再考虑代码层面的 import 修复。
  - 更完整的排障步骤见 `docs/build-troubleshooting.md`。

### 颜色规范（设计调色板）

界面颜色须按以下角色使用，确保一致性与可维护性。色值以深色模式为准；对应 CSS 变量见 `tailwind.css` 中 `.dark` 块。

| 角色 | 色码 (HEX) | RGB | 用途说明 | 推荐变量（深色） |
|------|------------|-----|----------|------------------|
| 主背景色 | `#0A0A0A` | 10, 10, 10 | 页面最底层背景，营造深邃感 | `--default-bg-color` |
| 容器/卡片背景 | `#18181B` | 24, 24, 27 | 各内容模块、卡片、图表的背景 | `--default-box-color` |
| 主品牌色/高亮 | `#3B82F6` | 59, 130, 246 | 关键指标、按钮、选中状态、图表主色 | `--art-primary` / `--el-color-primary` |
| 辅助色 1（增长/积极） | `#10B981` | 16, 185, 129 | 增长、健康、完成等积极状态的图表与指示灯 | `--art-success` |
| 辅助色 2（下降/警告） | `#F97316` | 249, 115, 22 | 下降、警告、待处理等中性或警示状态 | `--art-warning` |
| 辅助色 3（异常/危险） | `#EF4444` | 239, 68, 68 | 异常、失败、严重亏损等危险状态 | `--art-danger` / `--art-error` |
| 正文/标题文字 | `#F4F4F5` | 244, 244, 245 | 主要标题和段落文字 | `--art-gray-900` |
| 常规/辅助文字 | `#A1A1AA` | 161, 161, 170 | 辅助性说明、标签、图例文字 | `--art-gray-600` |
| 边框/分割线 | `#27272A` | 39, 39, 42 | 卡片边框、表格分割线 | `--default-border` / `--art-gray-300` |

- 写样式时**优先使用上表中的 CSS 变量**，避免硬编码 HEX；若需在 JS/图表配置中引用，可使用 `getComputedStyle(document.documentElement).getPropertyValue('--art-primary')` 等。
- 浅色模式由 `:root` 与现有 gray/primary 变量控制，与深色角色语义一致，具体色值见 `tailwind.css`。

### 语义文字颜色 Token

文字颜色优先使用语义变量而非直接引用灰阶（如用 `var(--text-secondary)` 而非 `var(--art-gray-600)`），亮/暗模式自动适配：

| 变量 | 指向 | 用途 | Tailwind 类名 |
|------|------|------|--------------|
| `--text-primary` | `--art-gray-900` | 标题、正文主文字 | `text-text-primary` |
| `--text-secondary` | `--art-gray-600` | 辅助说明、标签、图例 | `text-text-secondary` |
| `--text-tertiary` | `--art-gray-500` | 更弱的辅助信息 | `text-text-tertiary` |
| `--text-disabled` | `--art-gray-400` | 禁用态文字 | `text-text-disabled` |
| `--text-link` | `--art-primary` | 链接文字 | `text-text-link` |
| `--text-success` | `--art-success` | 积极/成功状态文字 | `text-text-success` |
| `--text-warning` | `--art-warning` | 警告状态文字 | `text-text-warning` |
| `--text-danger` | `--art-danger` | 危险/错误状态文字 | `text-text-danger` |

### 间距体系 Token

统一使用 `--space-*` 变量，避免散落魔法数字；Tailwind 中已映射为 `--spacing-*`：

| 变量 | 值 | 典型场景 |
|------|-----|----------|
| `--space-1` | 4px | 图标与文字间距 |
| `--space-2` | 8px | 紧凑元素间距、表单项内 |
| `--space-3` | 12px | 面板内部 padding |
| `--space-4` | 16px | 区块间距、栅格 gutter |
| `--space-5` | 20px | 中等区域间距 |
| `--space-6` | 24px | 大区块间距 |
| `--space-8` | 32px | 页面分区 |
| `--space-10` | 40px | 大间隔 |
| `--space-12` | 48px | 特大间隔 |
| `--space-16` | 64px | 页面级间距 |

### 阴影体系 Token

统一使用 `--shadow-*` 变量，亮/暗自动适配（暗色模式阴影更深）。Tailwind 中可直接用 `shadow-xs`~`shadow-xl`：

| 变量 | 用途 |
|------|------|
| `--shadow-xs` | 微弱阴影，输入框、小卡片 |
| `--shadow-sm` | 卡片默认阴影 |
| `--shadow-md` | 悬浮面板、下拉菜单 |
| `--shadow-lg` | 弹窗、抽屉 |
| `--shadow-xl` | 全屏浮层、大弹窗 |

### 动效 Token

过渡动画统一使用以下变量（**禁止** `transition: all`，须显式列出属性）：

| 变量 | 值 | 用途 |
|------|-----|------|
| `--duration-fast` | 150ms | hover 反馈、开关切换 |
| `--duration-normal` | 250ms | 面板展开、路由过渡 |
| `--duration-slow` | 350ms | 复杂动画、大面积过渡 |
| `--ease-default` | `cubic-bezier(0.4, 0, 0.2, 1)` | 通用缓动 |
| `--ease-in` | `cubic-bezier(0.4, 0, 1, 1)` | 元素退出 |
| `--ease-out` | `cubic-bezier(0, 0, 0.2, 1)` | 元素进入 |
| `--ease-spring` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 弹性效果 |

### Z-index 层级

z-index 统一使用变量，避免随意数值冲突：

| 变量 | 值 | 层级 |
|------|-----|------|
| `--z-dropdown` | 1000 | 下拉菜单 |
| `--z-sticky` | 1020 | 吸顶元素 |
| `--z-fixed` | 1030 | 固定定位 |
| `--z-modal-backdrop` | 1040 | 弹窗遮罩 |
| `--z-modal` | 1050 | 弹窗/抽屉 |
| `--z-popover` | 1060 | 气泡框 |
| `--z-tooltip` | 1070 | 工具提示 |
| `--z-toast` | 1080 | 消息提示 |
| `--z-fullscreen` | 2300 | 全屏容器 |
| `--z-progress-bar` | 2400 | 顶部进度条 |

### Focus 可见性

交互元素需要可见的 focus 状态。项目提供以下变量供业务组件使用 Tailwind `focus-visible:ring-*` 类：

- `--focus-ring-color`: `var(--art-primary)` — 焦点环颜色
- `--focus-ring-width`: `2px` — 焦点环宽度
- `--focus-ring-offset`: `2px` — 焦点环偏移

### 可访问性工具类

- `.tabular-nums`：对数字列/KPI 卡片等需要数字对齐的场景使用 `font-variant-numeric: tabular-nums`。
- **`prefers-reduced-motion`**：项目已全局响应用户的「减少动画」偏好，自动将动画/过渡缩短至近零。

## 其他

- **模块接口契约整理**：当需要 **按路由/页面梳理 `mock/backend-api`、与 `fetch*` / `config/data-source` 对齐、输出契约清单** 时，Agent 应遵循项目技能 **`.cursor/skills/module-api-contract-audit/SKILL.md`**，并配合规则 **`backend-fields.mdc`**、**`module-api-contract-audit.mdc`**、**`api-contract-and-mock-conventions.mdc`**、**`module-api-mock-config.mdc`**。契约须满足该 Skill「章节 0」：**`sampleResponse` 完整可联调**（含 **`api-contract-and-mock-conventions.mdc`**：**表格类主数组 ≥3 条**、**表列/树节点上闭合 `enum` 在示例中写全**）；**§0.4 / §0.5**：字段与 **`backend-fields`** 对齐、入参日期 **`startDate`/`endDate`**、同模块 **`sampleResponse` 形态尽量统一**；**§0.6（硬性）**：与 **`GET .../analysis/cockpit/meta-filter-options`**（无参）同构的顶栏公用下拉 **不** 再建模块内 `*meta-filter-options*.json`；页面 **直接读** **`useCockpitMetaFilterStore().data`**（或 **`useCockpitMetaFilterOptions().cockpitMeta`**），仅在 **`mock/backend-api/README.md`** 标注并引用 **`paid-analysis/mock/backend-api/README.md` 附录 A**（详见 **`api-contract-and-mock-conventions.mdc`**）。URL 按 **路由+模块+功能**（kebab-case）组织；**接口一律 POST + JSON 请求体**（与 `request.post` 一致；**例外**：该公用 meta 为 **GET 无参**，见 **`api-contract-and-mock-conventions.mdc`**）。**大模块多页面** 时另有 Skill「章节 9～10」：**契约 JSON 与开关聚合到模块根目录**（子页不单独建契约目录/config）、**`data-source` 须注释且多页面用多 enum / 多 `is*Mock` 方法**、**每 JSON 根级 `interaction`**、**README 场景→接口表**。
- **应用当前时间**：业务里表示「今天/此刻」或基于今天算日期时，**禁止**裸 `new Date()`，统一使用 `@/utils/app-now` 的 `getAppNow` / `cloneAppDate` / `offsetAppNowMs` 等；细则与例外见 **`app-now-date.mdc`**。
- **环境变量**: 以 `VITE_` 开头，在 `src/env.d.ts` 或代码中通过 `import.meta.env` 使用；勿提交敏感信息。
- **生产构建**: `pnpm build` 会先执行 `vue-tsc --noEmit`，再 Vite 构建；生产会 drop console/debugger。
- **Stylelint**: 遵循 `stylelint-config-recess-order` 等；Vue 内 `<style lang="scss">` 可与 Tailwind 混用。
