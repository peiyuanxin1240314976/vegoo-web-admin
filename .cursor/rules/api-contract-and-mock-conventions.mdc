---
description: 数据接口抽象与 Mock 契约（路由→模块→功能拆分，backend-api 文档与字段解释）
alwaysApply: true
---

# 数据接口抽象与 Mock 契约规范（必读）

本规则用于约束 **接口地址如何抽象**、**接口如何按模块/功能拆分**、以及 **Mock 契约如何沉淀到 `mock/backend-api`**，以保证后期可优化渲染、可拆分、可联调。

## 契约归属与前后端协作（必读）

- **`mock/backend-api/*.json` 为接口契约的源文件**：由**前端**在仓库内维护，在 JSON 中定义 `fieldDescription`（请求/响应字段说明）、`sampleRequest`、`sampleResponse`、`api`（路径、方法、requestBody/responseBody 摘要）等。**后端按该契约实现**接口路径、入参、出参结构与语义，**不是**由后端先定一套字段再要求前端改契约迁就。
- **联调顺序建议**：先有契约 JSON（及同目录 `README.md` 接口清单）→ 前端 `types.ts` / `src/api/*.ts` 与契约对齐 → 后端按 JSON 开发与自测 → 前后端联调。
- **不阻塞主流程（强制）**：对接/整理契约时，必须保证页面在 **接口未返回、返回为空、或请求失败** 的情况下依然能进入并完成主流程的浏览/操作路径（至少能看到完整 UI 框架 + 合理空态/占位），禁止把页面“是否可用”绑定到接口“必须有数据”这一前提上。
- **变更流程**：增删改字段或业务含义时，须**前后端协商一致**后，**优先更新契约 JSON**（及 README），再同步改前端类型与请求封装、Mock、后端实现；禁止长期存在「文档一套、接口一套、前端一套」。
- **字段与数据字典**：新建或修订契约 JSON 时，**逐字段**对照 `.cursor/rules/backend-fields.mdc`：`fieldDescription` 与 `sampleRequest` / `sampleResponse` 中的字段名、业务含义须可映射到字典；无法映射时须在 `_comment` 或 `mock/backend-api/README.md` 说明，并推动补全字典条目。
- **契约键名（驼峰 · 禁止下划线）**：`sampleRequest` / `sampleResponse` / `fieldDescription` 所涉**业务字段键名**须 **camelCase**，**不得**含 `_`（禁止 `s_*`、`n_*`、`date_start`、`request_quantity` 等）；细则见 **`backend-fields.mdc`**「接口契约与 HTTP JSON 命名」。
- **请求体日期范围**：凡表示查询/筛选时间范围的入参，键名须为 **`startDate`**、**`endDate`**（推荐值 `YYYY-MM-DD` 字符串，与模块约定一致）；详见 `backend-fields.mdc`「接口请求 · 日期范围」。
- **筛选入参枚举类型**：请求体键名须符合 `backend-fields.mdc` **「接口请求 · 统一筛选维度键名」**；广告平台用 **`source`**（**string**，如 `\"1\"`），**勿**在 `sampleRequest` 请求体中使用 `n_source` 作键名；**勿**写 JSON number。「全部」用 `\"\"` 等规则见 `backend-fields.mdc`「接口请求 · 筛选「全部」」与「接口请求 · 筛选字段类型（枚举 string）」。
- **应用 / 平台 / 国家 / 账户筛选键名**：凡页面或接口**实际提供**对应筛选时，请求体使用 **`appId`**、**`platform`**、**`countryCode`**、**`account`**（均为 `string`）及 **`source`**，键名与 `backend-fields.mdc`「接口请求 · 统一筛选维度键名」一致；**未**在 UI 出现的维度不得仅为对齐字典而写入契约或 POST body（见该节「与页面一致」）。
- 网关真实路径与示例 URL 不一致时，在对应 `mock/backend-api/README.md` 或 JSON `_comment` 中注明；以前端 `src/api` 实际请求为准联调。
- **公用 cockpit 顶栏 `meta-filter-options`（硬性要求）**：
  - **统一接口**：**`GET`** `/api/v1/datacenter/analysis/cockpit/meta-filter-options`（即 **`${ANALYSIS_API_BASE}/cockpit/meta-filter-options`**）。**无 query、无 body**，仅依赖全局 **`Authorization`**（与其它 GET meta 类接口一致）。这是全项目 **通用的顶栏筛选项下拉**（应用 / 终端平台 / 广告平台 / 国家等与 **`CockpitMetaFilterOptionsData`** 同构的选项）。与本文件下文「新契约优先 POST」为 **明确例外**。
  - **实现与类型**：HTTP 封装为 **`fetchCockpitMetaFilterOptions`**（`request.get`，`@/api/cockpit-meta-filter`），类型为 **`@/types/cockpit-meta-filter`** 之 **`CockpitMetaFilterOptionsData`**。业务 **POST** 与下拉联动的入参键名为 **`appId`** / **`platform`** / **`source`** / **`countryCode`** / **`account`**（见 **`backend-fields.mdc`**「接口请求 · 统一筛选维度键名」）及 **`paid-analysis/mock/backend-api/README.md` 附录 A**（指业务 POST 请求体，非本 GET）。
  - **页面与 Mock 开发（直接读数据，不要当业务接口再拉一遍）**：同构顶栏下拉的选项 **直接从 Pinia 读** **`useCockpitMetaFilterStore().data`**（或组合式 **`useCockpitMetaFilterOptions()`** 返回的 **`cockpitMeta`**），绑定 `appOptions` / `platformOptions` / `sourceOptions` / `countryOptions` 即可。**不要**在页面里把「再调一次 `fetchCockpitMetaFilterOptions`」当作常规写法。数据由 **路由守卫预加载** + **同标签 `sessionStorage`** 填充；**`ensureLoaded()`** 仅作兜底（例如极早挂载且 `data` 仍为空时），非日常入口。
  - **Mock / 契约（禁止重复 JSON）**：凡顶栏筛选与上述接口 **同构**（仅依赖 `appOptions`、`platformOptions`、`sourceOptions`、`countryOptions` 等公用形态），**不得**在该模块 `mock/backend-api` 再新增 `*meta-filter-options*.json` 或等价重复契约。**仅在** 模块 `mock/backend-api/README.md`（及需要时在 `mock/README.md`）中 **用表格一行 + 附录/引用** 标注：接口 URL、`fetch*` 名、**无独立 JSON**、说明详见 **付费分析 `backend-api/README.md` 附录 A**。整理契约时 **不** 为该机造「占位 JSON」凑数。
  - **例外**：若某页顶栏在公用四类之外还有 **本页独有** 筛选项（如转化类型、商品类型），**仅** 为 **独有维度** 维护独立契约 JSON（独立 endpoint）；**仍不得** 把与 cockpit 同构的四类选项再复制进该 JSON 或另建「整页 meta」重复契约。
  - **驾驶舱视图**：勿为 `src/views/cockpit` 在 mock 目录 **额外** 增改仅服务于同构顶栏的契约；顶栏联调以上述公用接口与类型为准。

## 七、非阻塞主流程与空数据策略（强制）

> 目标：接口联调期间/线上异常时，页面仍能展示与操作，不因“暂无数据”或“接口未就绪”阻塞主流程。

### 7.1 契约层（backend-api JSON）必须描述「空数据」与「失败兜底」

- **空数据形态（必须明确）**：对所有列表/表格/图表接口，在 `fieldDescription.response`（或 `apiSuggestion.notes`）中写清：
  - 数组字段在无数据时返回 `[]`（例如 `records/items/series/options`），而不是 `null`；
  - 计数字段在无数据时返回 `0`（例如 `total`）；
  - 可选字段缺失/空串的语义（例如筛选条件未命中、权限不足、后端尚未接入）。
- **示例覆盖**：
  - `sampleResponse` 仍以“可联调的完整示例”为主（尤其表格 ≥3 条）；
  - 若该接口存在关键 UI 分支依赖空态（例如“无匹配结果”“无权限”“首次进入无数据”），应在 `apiSuggestion` 中补充对应空态约定（必要时另加 `sampleResponseEmpty`（可选字段）描述空数据业务体结构）。
- **保持 shape 稳定**：即使无数据，也应返回与有数据时相同的业务体结构（同名键、同层级），避免前端分支判断复杂化。

### 7.2 前端对接（types / fetch / 页面）必须做到「不阻塞」

- **默认值兜底（必须）**：页面/组件状态初始化时给出可渲染的默认结构（如 `records: []`、`total: 0`、图表 `categories: [] / series: []` 等），避免 `undefined` 导致渲染报错。
- **加载与空态**：接口加载时允许展示骨架屏/占位；接口返回空数组时展示空态，但 **页面布局与筛选区/Tab/操作按钮** 不应消失。
- **错误降级**：请求失败应可提示错误（toast/空态文案均可），但不得阻塞用户继续切换 Tab、修改筛选或再次发起查询。

## 一、接口地址抽象：路由 → 模块 → 功能

### 1）先根据路由定义父级 API 地址

- **父级 API 路径**：与前端路由保持一致的层级结构（route path + 页面语义）。
- **示例**：
  - 路由：`/user-growth/ad-performance`
  - 父级 API：`/api/user-growth/ad-performance`

> 原则：**看到 API 路径即可反推页面路由**；避免一个“业务大杂烩”路径塞所有能力。

### 2）再根据项目模块定义分组（子路径）

在父级 API 下按页面内部模块进行分组（通常对应：筛选/概览/表格/详情/操作等），常见分组：

- `meta-*`：页面元信息/下拉选项/枚举字典
- `overview`：页面概览聚合（KPI/趋势/分布/预警/数据时间等）
- `table/<tab>`：主表格按 tab/维度拆分
- `<entity>-detail`：详情抽屉/弹窗（单一实体、维度清晰）
- `export`：导出
- `*-action`：操作类接口（暂停/启用/确认等）

### 3）最后根据具体功能定义 endpoint（kebab-case）

- endpoint 使用 **kebab-case**
- endpoint 命名必须体现“做什么”，不要使用含糊词（如 `getData` / `list`）
- **禁止**在单一 endpoint 中通过 `type/tab` 参数复用过多不同形态的响应；优先拆 endpoint（例如按 tab 拆 4 个接口）

## 二、接口拆分原则：禁止 1 个接口获取过多数据

### 目标

- 防止后期因单接口 payload 过大导致 **首屏/切换卡顿**、**渲染难优化**、**缓存粒度过粗**。

### 强约束

- **一个接口只服务一个明确的 UI 场景**（一个页面模块/一个 tab/一个抽屉）。
- **禁止**“一口气返回整页所有数据且不可按需裁剪”的设计。
- **禁止**把多种结构差异很大的数据塞进一个数组/对象里靠前端分流。

### 推荐拆分方式（按 UI 边界）

- **筛选项**：与同构驾驶舱顶栏一致时复用公用 **`.../cockpit/meta-filter-options`**（见上文「公用 cockpit 顶栏 meta-filter-options」）；**独有**维度再拆独立 `meta-*` endpoint。勿为前者再建模块内 `*meta-filter-options*.json`。
- **概览聚合**：`overview`（若仍过大，继续拆：`overview/kpi`、`overview/trend`、`overview/distribution`、`overview/alerts`）
- **表格**：按 tab/维度拆 `table/campaign`、`table/country`、`table/owner`、`table/account`
- **详情**：按实体 + 维度拆（例如 `campaign-detail`；若详情内含多 tab 且数据量大，可继续拆 `campaign-detail/ad-group`、`campaign-detail/date`、`campaign-detail/country`）
- **导出/操作**：独立接口（`export`、`alert-action` 等）

## 三、开发前必须定义：Mock 契约与字段解释（backend-api）

### 目录要求（每个业务页面/模块都要具备）

在对应页面目录下创建（或补齐）：

```
src/views/<模块>/<页面>/
  mock/
    README.md                 # mock 总说明（必需）
    backend-api/
      README.md               # 接口清单与 URL 说明（必需）
      01-*.json               # 按接口拆分的契约文件（必需）
      02-*.json
      ...
    api-mock/                 # （推荐）与各契约 sampleResponse 对齐的示例 JSON，便于对照与 MSW
      *.json
```

### “每个文件夹都要有 md 解释”的约束

- `mock/README.md`：说明本页面 mock 的用途、与类型/页面的关联、mock 数据来源等。
- `mock/backend-api/README.md`：说明 **父级 API 路径**、**接口清单（表格）**、**拆分原则**、优先级（P0/P1/P2）等。

> 若在 `mock/backend-api/` 下新增二级目录（如 `table/`、`detail/`），则该二级目录也必须有 `README.md`。

## 四、backend-api JSON 契约格式（必须包含字段解释）

每个接口对应一个 JSON 文件，至少包含：

> **硬性例外**：与篇首 **公用 cockpit 顶栏 `meta-filter-options`** 同构的能力 **不** 使用本目录 JSON，仅在 **`mock/backend-api/README.md`** 标注（见该节）。

- `_comment`：一句话描述接口与页面模块的对应关系（可引用对应类型）
- `fieldDescription`：
  - `request`：每个字段必须有 `type`、`required`、`description`；有枚举就写 `enum`；有默认值就写 `default`
  - `response`：顶层字段同样必须解释；复杂对象可引用类型名，并在同一文件中补充该类型的字段解释（或在子节点处给出 `fields`）
- `sampleRequest`：示例请求体（JSON）
- `sampleResponse`：示例响应体（JSON，若网关统一包裹则仅写 `data` 内结构或在说明中注明）
- `api`：接口元信息（url/method/priority/requestBody/responseBody）
- `apiSuggestion`：实现建议/注意点（可选但推荐）

### 字段解释的颗粒度要求

- **禁止**只写“见后端返回”或只给一个大对象示例不解释字段。
- 每个字段的 `description` 要能直接用于联调沟通：含义、单位、范围（例如百分比是 0-100 还是 0-1）、空值策略、格式（日期格式等）。
- **筛选类 `request` 字段**：若字段为枚举型筛选（如广告平台 **`source`**），`fieldDescription.request` 的 `type` 须为 **`string`**（键名驼峰，勿 `n_source`），且 `sampleRequest` 中的示例值类型须与 `type` 一致，详见 `backend-fields.mdc`「接口请求 · 筛选字段类型（枚举 string）」。

### 同模块 `sampleResponse` 形态复用

- 同一 `mock/backend-api` 目录（同一业务聚合单元）内，**同类 UI**（分页列表、KPI 卡片、趋势序列、meta 下拉等）的 **`sampleResponse` 业务体**尽量与存量契约 **结构一致**（键名层级、列表字段名、分页字段等），便于前端复用解析逻辑。
- 细则与整理时验收步骤见：`.cursor/skills/module-api-contract-audit/SKILL.md` **§0.5**；新增接口优先对齐本目录已有 JSON。

### 表格类 `sampleResponse` 示例条数（硬性）

- 若 **`sampleResponse` 含服务主界面数据表格** 的数组（常见键名：`records`、`items`、`rows`、`list`；或 `fieldDescription.response` 中明示为该接口主表格数据数组），则该数组内 **至少 3 条** 与 `fieldDescription` 对齐的 **完整** 示例项：可对照表头/列渲染，**禁止**用空对象、仅含主键字段或 `...` 占位糊弄联调。
- **树形表格**（含嵌套 `children`）：示例中用于表格展示、且结构完整的节点 **合计不少于 3 个**（可跨层级）；若业务上必须少于 3 条才能说明特殊结构，须在 `_comment` 或 `apiSuggestion` 中写明原因，并仍应优先对齐「≥3 条」以便 UI 联调。
- **不适用本条**：详情单对象、KPI/卡片、无表格数组的导出 URL/文件流响应、以及 **非表格主数据** 的辅助数组（如图例、单条趋势点）；**meta 下拉** 的 `options` 等 **不强制** 3 条（表格列若依赖其长度另有产品约定时除外）。

### 表格/列表示例中的枚举须写全（硬性）

- 凡 **`fieldDescription` 中对某字段列出闭合 `enum` 数组**（不含仅占位/口语描述的开放式取值），且该字段出现在 **`sampleResponse` 的主表格行 / 树表节点 / 主列表项**中，则示例 **在多行（或多节点）之间须使该字段的每一个枚举取值至少出现一次**（不要求枚举的笛卡尔积全排列，按列分别覆盖即可）。
- **`platform`、`status`、计费类型、树节点 `level`** 等影响 **标签颜色、禁用态、展开逻辑** 的字段，**必须**在示例中穷尽 `enum`；若未覆盖，视为契约未就绪。
- **meta 类 `options`**：若在选项项的 `value`（或同类字段）上声明了闭合 `enum`，**`sampleResponse` 中的 `options` 须包含全部 `value` 取值**（可与 label 一一对应），避免联调时漏状态。
- **例外**：`enum` 项过多（如 >15）或明确标注「引用数据字典动态扩展」的，须在 `_comment` 或 `apiSuggestion` 写明「示例仅覆盖 UI 分支必填子集」，并列出 **须在联调中覆盖的最小集合**；不得以例外为由长期省略 **状态/level** 等少数取值的示例。
- 与 **`backend-fields.mdc`** 中业务枚举一致时，契约 `enum` 与示例取值须可互证。

### JSON 模板（新接口可直接复制）

```json
{
  "_comment": "模块 - 功能说明。对应 types.ts: SomeType。",
  "fieldDescription": {
    "request": {
      "foo": { "type": "string", "required": true, "description": "字段含义。" },
      "bar": {
        "type": "string",
        "required": false,
        "enum": ["a", "b"],
        "default": "a",
        "description": "枚举含义与默认值。"
      }
    },
    "response": {
      "data": {
        "type": "SomeType",
        "required": true,
        "description": "响应主体（如网关包裹则为 data）。"
      }
    },
    "SomeType": {
      "id": { "type": "string", "required": true, "description": "主键/唯一标识。" },
      "name": { "type": "string", "required": true, "description": "名称。" }
    }
  },
  "sampleRequest": { "foo": "x", "bar": "a" },
  "sampleResponse": { "data": { "id": "1", "name": "demo" } },
  "api": {
    "url": "POST /api/<模块>/<页面>/<endpoint>",
    "method": "POST",
    "priority": "P0",
    "requestBody": "foo (必填), bar?",
    "responseBody": "SomeType"
  },
  "apiSuggestion": {
    "notes": ["补充实现建议、边界条件、格式约定等。"]
  }
}
```

## 五、与代码实现的绑定建议（落地）

- **类型先行**：接口契约字段需与页面 `types.ts`（或 `src/types/api/api.d.ts` 的 `Api.*`）保持一致，避免“文档一套、代码一套”。
- **请求方法命名**：在 `src/api/<业务>.ts` 内，方法名以 `fetch` 开头，体现模块与功能（如 `fetchAdPerformanceOverview`）。
- **字段命名**：涉及广告/增长/报表等概念时，字段命名与语义必须遵循 `backend-fields.mdc`（数据字典）。
- **Mock 与真实接口切换**：在**小模块** `views/<模块>/<页面>/config/` 中按**接口粒度**配置开关（推荐 `enum` + `Record`），并在 `src/api` 中写齐与契约一致的 `fetch*`；详见 `module-api-mock-config.mdc` 与 `project-conventions.mdc`「Mock 与数据源开关」。

## 六、新页面 / 新模块交付检查（简）

新建或大幅扩展 `src/views/<模块>/<页面>` 时，在编写页面与接口联调前应完成：

1. `mock/README.md`：说明用途、与 `types.ts` / 入口组件关系、数据从哪来（静态 / MSW / 聚合 `data.ts`）。
2. `mock/backend-api/README.md`：父级 API 路径（与路由层级一致）、接口清单表、拆分原则与 P0/P1。
3. `mock/backend-api/01-*.json` 起：一接口一文件，含完整 `fieldDescription`、`sampleRequest`、`sampleResponse`、`api`、`apiSuggestion`（推荐）；**若含主表格数据数组**，`sampleResponse` 须满足上文「表格类示例条数」及「表格/列表示例中的枚举须写全」。
4. （推荐）`mock/api-mock/*.json`：与各契约 `sampleResponse` 结构一致，便于复制到联调工具或接入 MSW。

参考实现：`src/views/user-growth/real-time-data/mock/`。

