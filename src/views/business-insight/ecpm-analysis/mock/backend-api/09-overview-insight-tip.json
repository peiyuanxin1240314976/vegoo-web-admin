{
  "_comment": "ECPM 分析 — 右侧运营提示条。对应 mock.ts: MOCK_ECPM_INSIGHT_TIP。",
  "fieldDescription": {
    "request": {
      "t_start_date": { "type": "string", "required": true, "description": "开始日 YYYY-MM-DD。" },
      "t_end_date": { "type": "string", "required": true, "description": "结束日 YYYY-MM-DD。" },
      "platform": { "type": "string", "required": true, "description": "终端平台。" },
      "appIds": {
        "type": "string[]",
        "required": true,
        "description": "应用筛选；不限为 []，单选为 [\"应用ID\"]。"
      }
    },
    "response": {
      "message": {
        "type": "string",
        "required": true,
        "description": "提示文案，单行或多行由前端样式控制。"
      }
    }
  },
  "sampleRequest": {
    "t_start_date": "2024-05-01",
    "t_end_date": "2024-05-31",
    "platform": "all",
    "appIds": []
  },
  "sampleResponse": {
    "message": "插屏式广告位普遍ECPM较高，建议优先保障展示频次"
  },
  "api": {
    "url": "POST /api/business-insight/ecpm-analysis/overview/insight-tip",
    "method": "POST",
    "priority": "P2",
    "requestBody": "日期范围 + 可选应用",
    "responseBody": "{ message: string }"
  },
  "apiSuggestion": {
    "notes": ["可由规则引擎或 AI 生成，字段保持 message 即可。"]
  }
}
