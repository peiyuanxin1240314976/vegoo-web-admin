{
  "_comment": "更新覆盖自动同步状态。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "汇率记录 ID。" },
      "overrideAuto": { "type": "boolean", "required": true, "description": "覆盖开关。" }
    },
    "response": {
      "saved": { "type": "boolean", "required": true, "description": "更新结果。" }
    }
  },
  "sampleRequest": { "id": "ER-001", "overrideAuto": true },
  "sampleResponse": { "data": { "saved": true } },
  "api": {
    "url": "POST /api/config-management/exchange-rate/override",
    "method": "POST",
    "priority": "P1",
    "requestBody": "id, overrideAuto",
    "responseBody": "{ saved }"
  }
}
