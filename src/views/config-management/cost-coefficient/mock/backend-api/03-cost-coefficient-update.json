{
  "_comment": "编辑成本系数。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "记录 ID（路径参数）。" },
      "nSource": { "type": "number", "required": false, "description": "广告平台 n_source。" },
      "tstart": { "type": "string", "required": false, "description": "生效日期。" },
      "dcostRatio": { "type": "number", "required": false, "description": "折算比例。" },
      "dinstallCost": { "type": "number", "required": false, "description": "安装成本。" },
      "remark": { "type": "string", "required": false, "description": "备注。" }
    },
    "response": {}
  },
  "sampleRequest": {
    "id": "CC-001",
    "tstart": "2026-04-01",
    "dcostRatio": 1.2,
    "dinstallCost": 0.35678,
    "remark": "季度更新"
  },
  "sampleResponse": { "data": {} },
  "api": {
    "url": "PUT /api/config-management/cost-coefficient/:id",
    "method": "PUT",
    "priority": "P0",
    "requestBody": "Partial<CostCoefficientFormModel>",
    "responseBody": "{}"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "编辑弹窗或抽屉保存 `updateCostCoefficient`。",
        "prerequisite": "与 `src/api` 一致为 PUT；长期可改为 POST `/update` + body 含 id。"
      }
    ],
    "defaultSelection": "无",
    "dedupe": "无"
  }
}
