{
  "_comment": "下载导入模板。",
  "fieldDescription": {
    "request": {
      "dataSource": { "type": "string", "required": false, "enum": ["appstore", "googleplay"], "description": "模板数据源类型。" }
    },
    "response": {
      "downloadUrl": { "type": "string", "required": true, "description": "模板下载地址。" }
    }
  },
  "sampleRequest": { "dataSource": "appstore" },
  "sampleResponse": { "data": { "downloadUrl": "/api/config-management/order-import/template?dataSource=appstore" } },
  "api": {
    "url": "GET /api/config-management/order-import/template",
    "method": "GET",
    "priority": "P1",
    "requestBody": "query: dataSource?",
    "responseBody": "{ downloadUrl }"
  }
}
