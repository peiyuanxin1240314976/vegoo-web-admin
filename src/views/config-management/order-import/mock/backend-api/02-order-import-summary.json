{
  "_comment": "今日导入统计。",
  "fieldDescription": {
    "request": {
      "dataSource": {
        "type": "string",
        "required": false,
        "enum": ["appstore", "googleplay"],
        "description": "按数据源过滤统计，不传或空字符串表示全部。"
      },
      "status": {
        "type": "string",
        "required": false,
        "enum": ["completed", "processing", "partial", "failed"],
        "description": "按状态过滤统计，不传或空字符串表示全部。"
      }
    },
    "response": {
      "todayTotal": { "type": "number", "required": true, "description": "今日任务数。" },
      "completed": { "type": "number", "required": true, "description": "完成数。" },
      "processing": { "type": "number", "required": true, "description": "进行中数。" },
      "failed": { "type": "number", "required": true, "description": "失败数。" }
    }
  },
  "sampleRequest": { "dataSource": "", "status": "" },
  "sampleResponse": { "data": { "todayTotal": 5, "completed": 3, "processing": 1, "failed": 1 } },
  "api": {
    "url": "POST /api/config-management/order-import/summary",
    "method": "POST",
    "priority": "P0",
    "requestBody": "{}",
    "responseBody": "{ todayTotal, completed, processing, failed }"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "页面首屏初始化",
        "detail": "列表页顶部 KPI 与表格并行 `fetchOrderImportSummary`。",
        "prerequisite": "`OrderImportListApiSource.importSummary`。"
      }
    ],
    "defaultSelection": "无请求参数。",
    "dedupe": "可与 table 并行。"
  }
}
