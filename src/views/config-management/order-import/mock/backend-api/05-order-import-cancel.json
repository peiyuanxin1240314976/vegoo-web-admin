{
  "_comment": "取消导入任务。",
  "fieldDescription": {
    "request": {
      "taskId": { "type": "string", "required": true, "description": "任务 ID。" }
    },
    "response": {
      "success": { "type": "boolean", "required": true, "description": "是否成功。" }
    }
  },
  "sampleRequest": { "taskId": "IMP-027" },
  "sampleResponse": { "data": { "success": true } },
  "api": {
    "url": "POST /api/config-management/order-import/cancel",
    "method": "POST",
    "priority": "P1",
    "requestBody": "{ taskId }",
    "responseBody": "{ success }"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "任务行「取消」`cancelOrderImport`。",
        "prerequisite": "`OrderImportListApiSource.cancelImport`。"
      }
    ],
    "defaultSelection": "无",
    "dedupe": "无"
  }
}
