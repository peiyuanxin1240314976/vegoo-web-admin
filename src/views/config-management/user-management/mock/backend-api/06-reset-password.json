{
  "_comment": "配置管理 - 重置密码。fetch*: resetUserPassword(id)。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "路径 :id。" }
    },
    "response": {
      "success": { "type": "boolean", "required": true, "description": "是否已触发重置流程。" },
      "message": { "type": "string", "required": false, "description": "提示信息。" }
    }
  },
  "sampleRequest": {},
  "sampleResponse": { "success": true, "message": "新密码或重置链接已发送至用户邮箱" },
  "api": {
    "url": "POST /api/config-management/user/:id/reset-password",
    "method": "POST",
    "priority": "P1",
    "requestBody": "无或空对象",
    "responseBody": "{ success, message? }"
  },
  "apiSuggestion": {
    "notes": ["实际可返回临时口令策略由安全团队约定；前端仅提示成功。"]
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "活跃用户行「重置密码」，确认后调用。",
        "prerequisite": "ElMessageBox 确认。"
      }
    ],
    "defaultSelection": "无",
    "dedupe": "无"
  }
}
