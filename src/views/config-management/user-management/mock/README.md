# 用户管理 · Mock 说明

- `data.ts`：列表种子数据 `USER_MOCK_SEED`、角色/应用选项；`cloneUserMockList()` 供页面初始化可变列表。
- `backend-api/`：与网关路径 `/api/config-management/user/*` 对应的契约说明。

页面类型见 `../types.ts`，请求封装见 `@/api/config-management`。
