# 角色管理 (Role) - Mock 契约说明

本目录包含了角色管理页面的 Mock 数据和接口契约。该页面主要分为左侧角色列表、中间权限配置（功能权限、数据权限）以及右侧该角色下用户列表。

- `backend-api/`: 存放后端接口的 JSON 契约文件，约束后端接口结构。
- `data.ts`: 前端静态假数据（接口未接入时的本地联调 fallback，后续对接完毕后废弃）。

## 联调指引

在后续联调过程中，将根据 `backend-api/` 下定义的接口契约封装 `src/api` 下的请求函数，并严格按契约进行前后端对齐。
