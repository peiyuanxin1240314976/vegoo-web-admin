{
  "_comment": "Ad account management - disable account.",
  "fieldDescription": {
    "request": {
      "id": { "type": "number", "required": true, "description": "Target account ID." }
    },
    "response": {
      "updated": { "type": "boolean", "required": true, "description": "Update result." },
      "id": { "type": "number", "required": true, "description": "Target account ID." },
      "status": {
        "type": "string",
        "required": true,
        "enum": ["enabled", "disabled"],
        "description": "Updated status. Success should return disabled."
      }
    }
  },
  "sampleRequest": {
    "id": 101
  },
  "sampleResponse": {
    "updated": true,
    "id": 101,
    "status": "disabled"
  },
  "api": {
    "url": "POST /api/config-management/ad-account-management/account/disable",
    "method": "POST",
    "priority": "P0",
    "requestBody": "id",
    "responseBody": "AdAccountStatusResult"
  },
  "apiSuggestion": {
    "notes": [
      "This action should only be used for enabled rows.",
      "Return a clear business message if the row is already disabled."
    ]
  },
  "interaction": {
    "triggers": [
      {
        "moment": "user action",
        "detail": "Called after user clicks disable and confirms the action."
      }
    ],
    "defaultSelection": "Use the current row id.",
    "dedupe": "Prevent repeated submit after confirmation."
  }
}
