{
  "_comment": "Ad account management - enable account.",
  "fieldDescription": {
    "request": {
      "id": { "type": "number", "required": true, "description": "Target account ID." }
    },
    "response": {
      "updated": { "type": "boolean", "required": true, "description": "Update result." },
      "id": { "type": "number", "required": true, "description": "Target account ID." },
      "status": {
        "type": "string",
        "required": true,
        "enum": ["enabled", "disabled"],
        "description": "Updated status. Success should return enabled."
      }
    }
  },
  "sampleRequest": {
    "id": 104
  },
  "sampleResponse": {
    "updated": true,
    "id": 104,
    "status": "enabled"
  },
  "api": {
    "url": "POST /api/config-management/ad-account-management/account/enable",
    "method": "POST",
    "priority": "P0",
    "requestBody": "id",
    "responseBody": "AdAccountStatusResult"
  },
  "apiSuggestion": {
    "notes": [
      "This action should only be used for disabled rows.",
      "Frontend may refresh the list, but the response is enough for local row update too."
    ]
  },
  "interaction": {
    "triggers": [
      {
        "moment": "user action",
        "detail": "Called after user clicks enable and confirms the action."
      }
    ],
    "defaultSelection": "Use the current row id.",
    "dedupe": "Prevent repeated submit after confirmation."
  }
}
