{
  "_comment": "导出绩效配置列表。",
  "fieldDescription": {
    "request": {
      "keyword": { "type": "string", "required": false, "description": "应用关键词。" },
      "appPlatform": { "type": "string", "required": false, "description": "平台筛选。" },
      "adPlatform": { "type": "string", "required": false, "description": "广告平台筛选。" },
      "status": { "type": "string", "required": false, "description": "状态筛选。" }
    },
    "response": {
      "fileToken": { "type": "string", "required": true, "description": "导出文件令牌。" }
    }
  },
  "sampleRequest": { "keyword": "", "appPlatform": "", "adPlatform": "", "status": "" },
  "sampleResponse": { "data": { "fileToken": "perf-config-export-20260324" } },
  "api": {
    "url": "POST /api/config-management/perf-config/export",
    "method": "POST",
    "priority": "P1",
    "requestBody": "筛选参数",
    "responseBody": "{ fileToken }"
  }
}
