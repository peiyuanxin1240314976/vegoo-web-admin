{
  "_comment": "绩效配置列表。",
  "fieldDescription": {
    "request": {
      "page": { "type": "number", "required": true, "description": "页码。", "default": 1 },
      "pageSize": { "type": "number", "required": true, "description": "每页条数。", "default": 20 },
      "keyword": { "type": "string", "required": false, "description": "应用关键词。" },
      "appPlatform": { "type": "string", "required": false, "description": "平台。" },
      "adPlatform": { "type": "string", "required": false, "description": "广告平台。" },
      "status": { "type": "string", "required": false, "description": "状态。" }
    },
    "response": {
      "total": { "type": "number", "required": true, "description": "总数。" },
      "list": { "type": "PerfConfigItem[]", "required": true, "description": "配置列表。" }
    }
  },
  "sampleRequest": { "page": 1, "pageSize": 20, "keyword": "", "appPlatform": "", "adPlatform": "", "status": "" },
  "sampleResponse": { "data": { "total": 1, "list": [{ "id": "PF-001", "appName": "BloodSugar" }] } },
  "api": {
    "url": "POST /api/config-management/perf-config/table",
    "method": "POST",
    "priority": "P0",
    "requestBody": "列表筛选参数",
    "responseBody": "{ total, list }"
  }
}
