{
  "_comment": "归档版本。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "配置 ID（路径参数）。" },
      "version": { "type": "number", "required": true, "description": "版本号。" }
    },
    "response": {
      "archivedVersion": { "type": "number", "required": true, "description": "已归档版本号。" }
    }
  },
  "sampleRequest": { "id": "PF-001", "version": 1 },
  "sampleResponse": { "data": { "archivedVersion": 1 } },
  "api": {
    "url": "POST /api/config-management/perf-config/:id/archive",
    "method": "POST",
    "priority": "P1",
    "requestBody": "version",
    "responseBody": "{ archivedVersion }"
  }
}
