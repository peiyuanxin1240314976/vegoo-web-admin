{
  "_comment": "开户管理 - 导出开户记录列表。",
  "fieldDescription": {
    "request": {
      "keyword": { "type": "string", "required": false, "description": "关键词筛选。" },
      "source": { "type": "string", "required": false, "description": "广告平台筛选。" },
      "status": { "type": "string", "required": false, "description": "状态筛选。" },
      "agency": { "type": "string", "required": false, "description": "代理商筛选。" },
      "appIds": {
        "type": "string[]",
        "required": false,
        "description": "应用筛选 ID 列表（当前页面仅支持单选，最多传 1 个）。"
      }
    },
    "response": {
      "taskId": { "type": "string", "required": true, "description": "导出任务 ID。" }
    }
  },
  "sampleRequest": { "keyword": "", "source": "", "status": "", "agency": "", "appIds": [] },
  "sampleResponse": {
    "code": 200,
    "message": "success",
    "data": { "taskId": "export_open_account_20260324_001" }
  },
  "api": {
    "url": "POST /api/config-management/open-account/export",
    "method": "POST",
    "priority": "P1",
    "requestBody": "Partial<OpenAccountTableQuery>",
    "responseBody": "{ taskId } | blob"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "开户 Tab 导出 `exportOpenAccountList`。",
        "prerequisite": "`AccountApiSource.exportOpenAccount`。"
      }
    ],
    "defaultSelection": "与列表筛选一致。",
    "dedupe": "无"
  }
}
