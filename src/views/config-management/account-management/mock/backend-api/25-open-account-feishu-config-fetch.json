{
  "_comment": "开户管理 - 读取飞书推送设置。",
  "fieldDescription": {
    "request": {},
    "response": {
      "enabled": { "type": "boolean", "required": true, "description": "当前是否开启推送。" },
      "webhook": { "type": "string", "required": false, "description": "已配置 webhook。" },
      "mentionAll": { "type": "boolean", "required": false, "description": "是否 @所有人。" }
    }
  },
  "sampleRequest": {},
  "sampleResponse": {
    "code": 200,
    "message": "success",
    "data": { "enabled": true, "webhook": "https://open.feishu.cn/xxx", "mentionAll": false }
  },
  "api": {
    "url": "POST /api/config-management/open-account/feishu-config",
    "method": "POST",
    "priority": "P1",
    "requestBody": "{}",
    "responseBody": "{ enabled, webhook?, mentionAll? }"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "页面首屏初始化",
        "detail": "「开户管理」Tab 打开飞书设置或进入 Tab 时 `fetchOpenAccountFeishuConfig`；`fetchOpenAccountFeishuConfig === false` 在源码语义为跳过远程（见 `open-account-tab`）。",
        "prerequisite": "与 `AccountApiSource.fetchOpenAccountFeishuConfig` 对齐阅读。"
      }
    ],
    "defaultSelection": "无",
    "dedupe": "无"
  }
}
