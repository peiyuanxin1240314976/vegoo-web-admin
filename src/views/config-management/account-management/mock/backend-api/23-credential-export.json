{
  "_comment": "凭据管理 - 导出凭据列表。",
  "fieldDescription": {
    "request": {
      "keyword": { "type": "string", "required": false, "description": "关键词筛选。" },
      "source": { "type": "string", "required": false, "description": "广告平台筛选。" },
      "credentialType": { "type": "string", "required": false, "description": "凭据类型筛选。" },
      "status": { "type": "string", "required": false, "description": "验证状态筛选。" }
    },
    "response": {
      "taskId": { "type": "string", "required": true, "description": "导出任务 ID。" }
    }
  },
  "sampleRequest": { "keyword": "", "source": "", "credentialType": "", "status": "" },
  "sampleResponse": {
    "code": 200,
    "message": "success",
    "data": { "taskId": "export_credential_20260324_001" }
  },
  "api": {
    "url": "POST /api/config-management/credential/export",
    "method": "POST",
    "priority": "P1",
    "requestBody": "筛选参数",
    "responseBody": "{ taskId } | blob"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "凭据 Tab 导出 `exportCredentialList`。",
        "prerequisite": "`AccountApiSource.exportCredential`。"
      }
    ],
    "defaultSelection": "与当前筛选一致。",
    "dedupe": "无"
  }
}
