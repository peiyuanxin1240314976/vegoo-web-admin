{
  "_comment": "代理商管理 - 删除代理商。id 放入请求体，使用 POST。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "代理商ID，如 AGY-001。" }
    },
    "response": {
      "data": { "type": "null", "description": "成功时 data 为 null，依赖 HTTP 200 判断。" }
    }
  },
  "sampleRequest": {
    "id": "AGY-001"
  },
  "sampleResponse": {
    "data": null
  },
  "api": {
    "url": "POST /api/config-management/agency/delete",
    "method": "POST",
    "priority": "P0",
    "requestBody": "{ id }",
    "responseBody": "null"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "删除确认后 `deleteAgency`，刷新列表。",
        "prerequisite": "`AccountApiSource.deleteAgency`。"
      }
    ],
    "defaultSelection": "无",
    "dedupe": "无"
  }
}
