{
  "_comment": "凭据管理 - 删除凭据。id 放入请求体，使用 POST。删除前前端需用户输入凭据名称二次确认。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "凭据ID，如 CRED-001。" }
    },
    "response": {
      "data": { "type": "null", "description": "成功时 data 为 null。" }
    }
  },
  "sampleRequest": { "id": "CRED-001" },
  "sampleResponse": { "data": null },
  "api": {
    "url": "POST /api/config-management/credential/delete",
    "method": "POST", "priority": "P1",
    "requestBody": "{ id }",
    "responseBody": "null"
  }
}
