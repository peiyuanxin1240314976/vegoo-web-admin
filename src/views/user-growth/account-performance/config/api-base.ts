/**
 * 账户成效模块专用 API 路径前缀。
 * 须带 `account-performance` 段，勿与 `ANALYSIS_API_MIDDLE_PREFIX === ''` 时的扁平路径混用。
 */
export const ACCOUNT_PERFORMANCE_API_BASE = '/api/v1/datacenter/analysis/account-performance'
