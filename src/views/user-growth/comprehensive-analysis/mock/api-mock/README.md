# 综合分析 — api-mock 示例 JSON

与 `../backend-api/02`～`08` 中 **`sampleResponse` 的业务体**一致（根级 `data` 模拟网关包裹），便于 Postman / MSW 导入。

**筛选项**：不在此目录；见公用 **`GET .../cockpit/meta-filter-options`** 与 **`useCockpitMetaFilterStore`**。

**维护**：调整 `mock/data.ts` 基线或契约示例后，请同步更新本目录对应 `02`～`08` 文件。

**POST Mock 开关**：`../config/data-source.ts`；实现为 `../comprehensive-analysis-api-mock.ts`。
