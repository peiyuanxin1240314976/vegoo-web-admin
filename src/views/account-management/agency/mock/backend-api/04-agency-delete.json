{
  "_comment": "账户管理 · 代理商 — 删除。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "代理商 ID。" }
    },
    "response": {
      "data": {
        "id": { "type": "string", "description": "已删除的代理商 ID。" },
        "deleted": { "type": "boolean", "description": "是否删除成功。" }
      }
    }
  },
  "sampleRequest": {
    "id": "AGY-007"
  },
  "sampleResponse": {
    "data": {
      "id": "AGY-007",
      "deleted": true
    }
  },
  "api": {
    "url": "POST /api/config-management/agency/delete",
    "method": "POST",
    "priority": "P0",
    "requestBody": "{ id }",
    "responseBody": "{ id, deleted }"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "点击删除后进入确认弹窗，确认后调用 `deleteAgency`。"
      }
    ],
    "defaultSelection": "无",
    "dedupe": "无"
  }
}
