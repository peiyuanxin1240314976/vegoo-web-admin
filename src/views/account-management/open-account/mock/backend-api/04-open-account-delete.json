{
  "_comment": "开户页面 - 删除开户记录。",
  "fieldDescription": {
    "request": {
      "id": { "type": "string", "required": true, "description": "开户记录 ID。" }
    },
    "response": {
      "success": { "type": "boolean", "required": true, "description": "删除是否成功。" }
    }
  },
  "sampleRequest": {
    "id": "OA-103"
  },
  "sampleResponse": {
    "code": 200,
    "message": "success",
    "data": {
      "success": true
    }
  },
  "api": {
    "url": "POST /api/account-management/open-account/delete",
    "method": "POST",
    "priority": "P0",
    "requestBody": "{ id }",
    "responseBody": "{ success }"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "用户操作",
        "detail": "点击删除并二次确认后调用。",
        "prerequisite": "存在有效记录 id。"
      }
    ],
    "defaultSelection": "无。",
    "dedupe": "重复删除返回幂等成功或明确错误码。"
  }
}
