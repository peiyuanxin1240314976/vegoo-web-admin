{
  "_comment": "开户页面 - 飞书推送配置读取。",
  "fieldDescription": {
    "request": {},
    "response": {
      "enabled": { "type": "boolean", "required": true, "description": "是否开启飞书推送。" },
      "webhook": { "type": "string", "required": false, "description": "机器人 webhook 地址。" },
      "mentionAll": { "type": "boolean", "required": false, "description": "是否 @所有人。" }
    }
  },
  "sampleRequest": {},
  "sampleResponse": {
    "code": 200,
    "message": "success",
    "data": {
      "enabled": true,
      "webhook": "https://open.feishu.cn/open-apis/bot/v2/hook/abcdefg",
      "mentionAll": false
    }
  },
  "api": {
    "url": "POST /api/account-management/open-account/feishu-config/fetch",
    "method": "POST",
    "priority": "P1",
    "requestBody": "{}",
    "responseBody": "{ enabled, webhook?, mentionAll? }"
  },
  "interaction": {
    "triggers": [
      {
        "moment": "页面初始化",
        "detail": "进入页面后用于展示当前飞书推送状态。",
        "prerequisite": "无。"
      }
    ],
    "defaultSelection": "无。",
    "dedupe": "可与列表请求并行。"
  }
}
