# 开户主体配置 Mock 说明

当前页面 `src/views/account-management/open-account/index.vue` 使用的本地 Mock 资产如下：

- `data.ts`：主体配置静态示例数据。
- `subject-setting-api-mock.ts`：列表查询、主体保存、平台开关切换。
- `backend-api/`：与当前页面能力对应的契约 JSON（列表、保存、平台开关）与目录说明。
