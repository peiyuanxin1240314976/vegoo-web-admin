# 字段 → 契约索引（可选维护）

本文件**不是**第二套数据字典；字段含义以 [`.cursor/rules/backend-fields.mdc`](../.cursor/rules/backend-fields.mdc) 为准。

在按需做跨模块检索时，可在此记录：**标准字段名**（或 `backend-fields` 表中条目）与 **典型出现位置**（`mock/backend-api` 契约文件名、`fetch*` 等），便于联调与重构时查找。

## 表头模板

| 字段 / 字典条目         | 含义（链到表中业务概念即可） | 典型契约 JSON 或 API |
| ----------------------- | ---------------------------- | -------------------- |
| `startDate` / `endDate` | 请求体日期范围               | （待填）             |

整理某模块契约时，可在 Skill 交付物中增量建议是否写入本表；不必一次求全。
