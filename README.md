# web-vegoo-admin

## 环境要求

- Node.js >= 20.19.0
- pnpm >= 8.8.0

## 安装依赖

```bash
pnpm install
```

## 开发

```bash
pnpm dev
```

## 打包

```bash
pnpm build
```

执行 `vue-tsc --noEmit` 进行类型检查，通过后使用 Vite 构建生产版本，产物输出到 `dist` 目录。

## 预览打包结果

```bash
pnpm serve
```

在本地启动静态服务预览打包后的站点。
